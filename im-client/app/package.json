{
  "name": "im-client-app",
  "version": "1.0.0",
  "private": true,
  "description": "即时通讯手机版（Capacitor 打包）",
  "scripts": {
    "cap:sync": "cap sync android",
    "cap:open": "cap open android",
    "build": "cap sync android && cd android && gradlew.bat assembleRelease"
  },
  "dependencies": {
    "@capacitor/android": "^6.2.2",
    "@capacitor/cli": "^6.2.2",
    "@capacitor/core": "^6.2.2"
  }
}
